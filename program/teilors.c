#include <stdio.h>
#include <math.h>

double x, y, S, a;
int k = 0;

int main()
{

   printf("ievadi savu x vērtību\n");
   scanf("%lf\n",&x1);

   a = pow(x,k)/factorial(2*k);
   S = a;
   printf ("x = %f, a = %f, S = %f",x,a,S);

   

return 0;
}
